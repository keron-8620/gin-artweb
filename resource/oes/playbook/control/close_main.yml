---
- hosts: all

  vars_files:
    - ../common/base_vars.yml

  tasks:
    - name: oes close
      command: ./oes close
      args:
        chdir: "{{ slave_path_oes_home_bin }}"
      when: node_role == "master"
