## 测试计划

### 1. 测试CreateModel方法
- **正常场景**：创建登录记录模型并验证创建成功
- **异常场景**：传入nil模型参数，验证返回错误

### 2. 测试ListModel方法
- **正常场景**：创建多条登录记录并验证列表查询成功
- **分页测试**：验证分页查询功能
- **排序测试**：验证排序功能

### 3. 测试GetLoginFailNum方法
- **正常场景**：验证获取不存在IP的登录失败次数时返回maxNum
- **异常场景**：传入空IP地址，验证返回错误

### 4. 测试SetLoginFailNum方法
- **正常场景**：设置登录失败次数并验证设置成功
- **异常场景**：传入空IP地址，验证返回错误

### 5. 上下文相关测试
- **取消上下文**：测试上下文被取消时的行为
- **超时上下文**：测试上下文超时时的行为

### 6. 缓存相关测试
- **缓存过期**：测试缓存过期后获取登录失败次数的行为

## 实现细节
- 参考api_repo_test.go的测试结构和风格
- 使用test.NewTestGormDBWithConfig创建测试数据库
- 使用test.NewTestZapLogger创建测试日志记录器
- 使用context.Background()创建测试上下文
- 使用suite.NoError和suite.Error等断言方法验证测试结果

## 测试文件结构
- 保持现有的RecordTestSuite结构
- 为每个方法添加对应的测试函数
- 确保测试覆盖所有主要功能和边界情况